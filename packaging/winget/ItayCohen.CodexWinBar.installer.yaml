# yaml-language-server: $schema=https://aka.ms/winget-manifest.installer.1.6.0.schema.json
PackageIdentifier: ItayCohen.CodexWinBar
PackageVersion: 1.0.0
InstallerType: exe
Scope: user
InstallModes:
  - silent
  - silentWithProgress
  - interactive
InstallerSwitches:
  Silent: "--silent"
  SilentWithProgress: "--silent"
UpgradeBehavior: install
# Lets `winget uninstall` / list find the app via its Add/Remove Programs entry (Velopack registers it).
AppsAndFeaturesEntries:
  - DisplayName: CodexWinBar
    Publisher: Itay Cohen
Installers:
  - Architecture: x64
    InstallerUrl: https://github.com/ItayCohen-Prog/CodexWinBar/releases/download/v1.0.0/CodexWinBar-win-Setup.exe
    # IMPORTANT: this MUST be the SHA256 of the PUBLISHED release asset, not a local build.
    # After the release exists, recompute and paste it (see packaging/winget/README.md).
    InstallerSha256: 49071858B7F1AE18BB5D35F7DACC174ECB52CA3AED245E818E3286FBCC8A6109
ManifestType: installer
ManifestVersion: 1.6.0
