# Windows port versioning. The build's real source of truth is <Version> in Directory.Build.props;
# this file is retained for upstream (macOS) tooling compatibility and kept in sync.
MARKETING_VERSION=1.1.20
BUILD_NUMBER=1
